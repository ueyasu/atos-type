# 小学生向けタイピングゲーム アトス バトルタイピング

このプロジェクトは、小学生向けタイピングゲーム「アトス バトルタイピング」のプロジェクトである。
実装の詳細（技術スタック・ディレクトリ構成・エンジン仕様など）は [docs/implementation.md](docs/implementation.md) を参照。

## ゲームの概要

- 日本の小学生に向けたもので、ローマ字の習熟とタイピングの正確性を目標にする。タイピング速度も評価には入れるが、重視はしない
- Webアプリケーションとしてブラウザで動作するゲームとする。スコア記録などサーバでデータを持つ必要は無い。機微情報は扱わず、ブラウザ内のsession storageやlocal storageを利用する
- シーンは以下を持つ
    1. タイトル画面
        - アトス バトルタイピング というタイトルを表示。スペースキーでメニュー画面に遷移
    2. メニュー画面
        - 「ゲームスタート」と「設定」のボタンを表示。ゲームスタートボタンで難易度選択画面に遷移。設定ボタンで設定画面に遷移
    3. 設定画面
        - 「じゃ」を zya とするか ja とするか、などを設定
    4. 難易度選択画面
        - 難易度選択ボタンで難易度を選択。ボタンで選択したらタイピング画面に遷移する
            - かんたん: 一文字のローマ字入力
            - ふつう: 「おはよう」「さようなら」など、5文字以下の簡単な日常文言。濁音、半濁音などは入れない
            - むずかしい: 「じゅぎょう」など、単語だが濁音・半濁音、「ゃ」「ゅ」「ょ」を含んだ単語
    5. タイピング画面
        - サイドビューのファンタジーJRPG (ファイナルファンタジーシリーズ) のようなインタフェースを持つ
        - ユーザが1キーをタイピングするごとに、主人公の剣士が攻撃するモーションが発生
        - 時間経過やミスタイプで敵の攻撃が発生し、主人公のHPが減少。ゼロになったときスコア表示画面に遷移する
        - 敵キャラクターは5体用意する。スライム、ゴブリン、スケルトン、ゴーレム、ドラゴンと遷移。ゴーレムとドラゴンは一つ高い難易度の問題を出す。むずかしいの場合は7文字以上の単語を出す。5体すべて倒した場合はゲームクリアと表示して、スコア表示画面に遷移する
    6. スコア表示画面
        - タイプした文字数、正確性 (誤タイプ結果からパーセントで算出)、クリアまでの秒数を表示

## コーディングルール

- Do not preserve backward compatibility.
- Choose the simplest implementation that fully meets the current requirements.
- Prefer established, well-maintained libraries over custom implementations.
- Make architectural decisions for the long term. Do not accept a stopgap that only works for now and is meant to be replaced later.

## グラフィックデザイン

- **アートスタイル**
    - 小学生向けに親しみやすい**フラットデザイン（ベクター調）**を採用する。
    - アセット作成の負担を最小限にするため、キャラクター（主人公・敵）はコマ送りアニメーション用の画像を持たず、**「1キャラクターにつき1〜数枚の静止画」**で構成する。
- **アニメーション手法（Tweenアニメーション）**
    - 画像の切り替え（コマ送り）ではなく、プログラムによる画像の移動・回転・拡縮・色調変化（Tween）でアニメーションを表現する（ペーパーマリオ風のスタイル）。
    - **待機（呼吸）**: Y軸方向にゆっくり拡縮（Squash & Stretch）させる。
    - **攻撃**: 前方に素早く移動（Translate X）し、少し回転（Rotate）して戻る。同時にフリー素材などの斬撃エフェクトを重ねる。
    - **被ダメージ**: 画像全体を一瞬赤く染め（Color Filter）、左右に激しく振動（Shake）させる。
- **画面構成（タイピング画面）**
    - **上部（バトルエリア）**: 左側に主人公、右側に敵を配置。
    - **中段（ステータスエリア）**: 主人公と敵のHPバーを大きく表示。
    - **下部（タイピングエリア）**: 出題される文字列と、ローマ字の入力ガイドを表示。入力済みの文字は色を変えて視認性を高める。
    - バトルエリアには背景を描画すること。スライム・ゴブリンは森、スケルトンとゴーレムは城内、ドラゴンは城の上をイメージ

## アーキテクチャ

- **技術スタック**: React + TypeScript + Vite / PixiJS（バトル描画）/ GSAP（Tweenアニメーション）/ Tailwind CSS（スタイリング）/ Zustand（状態管理）。詳細は docs §1 を参照。
- **コアモジュール設計**
    - **Scene Manager (React)**: 各画面コンポーネントを切り替える。
    - **Battle Stage Component (PixiJS + GSAP)**:
        - Reactコンテナ内にPixiJSキャンバスを展開。
        - Zustandのステートを監視し、イベントに応じてGSAPでアニメーションを発火させる。
            - 例: `heroAttackSeq` カウンタが増加したら、GSAPで主人公のX座標を右に動かし、元の位置に戻す。
    - **Typing Engine (タイピング判定)**:
        - タイピング判定機構を実装する。設定画面の「ローマ字揺れルール」を注入し、正誤判定を行う
        - ひらがな→ローマ字のモーラ変換テーブルから正解の全表記（訓令式/ヘボン式、`x` 付き拗音、促音の二重子音化、長音、`ん`）を生成し、前置一致で正誤判定する
    - **Game Loop / Timer**:
        - 敵の攻撃までの時間制限（タイムプログレスバーなど）や、時間経過によるダメージ処理を管理するフック（`useBattleTimer`等）。
- **データ永続化（ストレージ）**
    - `localStorage` を使用（設定情報、ハイスコアなど。機微情報は扱わない）。

### ディレクトリ構成

現状の構成は [docs/implementation.md](docs/implementation.md) §3 を参照。要点は以下のとおり。

- `src/scenes/` … 各画面（タイトル・メニュー・設定・難易度・バトル・スコア）
- `src/pixi/` … バトル描画エンジン（PixiJS + GSAP）
- `src/lib/typingEngine.ts` … ローマ字タイピング判定エンジン（React非依存）
- `src/hooks/` … タイピング・バトル進行ロジック
- `src/store/` … Zustand 状態管理（localStorage 永続化）
- `src/data/` … 単語・敵のマスターデータ
- `src/assets/` … 画像（images/）と効果音（se/）
- `test-engine.ts` … タイピングエンジン単体テスト（`node test-engine.ts`）


